spring.application.name=appointment-system

spring.datasource.url=jdbc:mysql://localhost:3306/academic_counseling_db?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Aqil2612#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# --- JWT Configuration ---
# IMPORTANT: This secret MUST be the same as the one used in your authentication service
# It should be a strong, base64-encoded string (at least 32 characters long before encoding for HS256)
# Example: Generate a random key (e.g., in Java: Base64.getEncoder().encodeToString(Keys.secretKeyFor(SignatureAlgorithm.HS256).getEncoded()))
jwt.secret=cQ7j6sZbPOT1Jh4e0YZ7Tx3M0f1ZZ1m0DXqkTnVOWUQ=
jwt.expiration.ms=3600000 # 1 hour (in milliseconds)

# --- Logging Configuration ---
logging.level.org.springframework=DEBUG
logging.level.org.hibernate=DEBUG

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=aqilahrosidi@gmail.com
spring.mail.password=ltmk dmqm vgsm fpjd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true